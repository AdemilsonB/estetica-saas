// placeholder temporário — será substituído na próxima tarefa
'use client'
type Props = { open: boolean; onClose: () => void; onCreated?: (id: string) => void }
export function CreateCustomerModal(_: Props) { return null }
